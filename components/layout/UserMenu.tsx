import { LogoutButton } from "@/app/components/logout-button";
export function UserMenu({ email, displayName, compact = false }: { email: string; displayName?: string; compact?: boolean }) { const name = displayName?.trim() || email.split("@")[0] || "Abenteurer"; return <div className={`border-t border-[var(--mythoria-border)] ${compact ? "p-4" : "p-5"}`}><div className="flex min-w-0 items-center gap-3"><span aria-hidden="true" className="flex h-10 w-10 shrink-0 items-center justify-center rounded-xl border border-[var(--mythoria-border-gold)] bg-[var(--mythoria-green-dark)]/40 font-fantasy font-bold text-[var(--mythoria-neon-soft)]">{name.slice(0,1).toUpperCase()}</span><div className="min-w-0 flex-1"><p className="truncate text-sm font-bold text-[var(--mythoria-text)]">{name}</p><p className="truncate text-xs text-[var(--mythoria-text-muted)]">{email}</p></div></div><LogoutButton /></div>; }
