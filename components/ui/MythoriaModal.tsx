"use client";
import {useEffect,useRef,type ReactNode} from "react";
type Props={open:boolean;title:string;description?:string;onClose:()=>void;children?:ReactNode;footer?:ReactNode};
export function MythoriaModal({open,title,description,onClose,children,footer}:Props){const closeRef=useRef<HTMLButtonElement>(null);const dialogRef=useRef<HTMLElement>(null);useEffect(()=>{if(!open)return;const previous=document.activeElement instanceof HTMLElement?document.activeElement:null;closeRef.current?.focus();function key(event:KeyboardEvent){if(event.key==="Escape")onClose();if(event.key!=="Tab"||!dialogRef.current)return;const elements=dialogRef.current.querySelectorAll<HTMLElement>('button:not([disabled]),a[href],input:not([disabled]),select:not([disabled]),textarea:not([disabled]),[tabindex]:not([tabindex="-1"])');if(elements.length===0)return;const first=elements[0],last=elements[elements.length-1];if(event.shiftKey&&document.activeElement===first){event.preventDefault();last.focus()}else if(!event.shiftKey&&document.activeElement===last){event.preventDefault();first.focus()}}document.addEventListener("keydown",key);return()=>{document.removeEventListener("keydown",key);previous?.focus()}},[open,onClose]);if(!open)return null;return <div className="fixed inset-0 z-[100] flex items-center justify-center bg-black/75 p-4" role="presentation" onMouseDown={event=>{if(event.currentTarget===event.target)onClose()}}><section ref={dialogRef} role="dialog" aria-modal="true" aria-labelledby="mythoria-modal-title" aria-describedby={description?"mythoria-modal-description":undefined} className="mythoria-panel max-h-[90vh] w-full max-w-xl overflow-y-auto p-6"><div className="flex items-start justify-between gap-4"><div><h2 id="mythoria-modal-title" className="mythoria-subheading text-2xl">{title}</h2>{description&&<p id="mythoria-modal-description" className="mt-2 text-sm text-[var(--mythoria-text-muted)]">{description}</p>}</div><button ref={closeRef} type="button" onClick={onClose} aria-label="Dialog schließen" className="mythoria-focus-ring min-h-11 min-w-11 rounded-xl border border-[var(--mythoria-border)] text-xl text-[var(--mythoria-text-secondary)] hover:bg-[var(--mythoria-panel-hover)]">×</button></div>{children&&<div className="mt-6">{children}</div>}{footer&&<footer className="mt-6 flex flex-wrap justify-end gap-3 border-t border-[var(--mythoria-border)] pt-5">{footer}</footer>}</section></div>}
