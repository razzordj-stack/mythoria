import type { ReactNode } from "react";
export function MythoriaEmptyState({ icon = "◇", title, description, action, className = "" }: { icon?: ReactNode; title: string; description: string; action?: ReactNode; className?: string }) { return <section className={["mythoria-panel px-6 py-14 text-center", className].join(" ")}><span aria-hidden="true" className="mx-auto flex h-16 w-16 items-center justify-center rounded-2xl border border-[var(--mythoria-border-gold)] bg-[var(--mythoria-green-dark)]/40 text-3xl text-[var(--mythoria-neon-soft)]">{icon}</span><h2 className="mythoria-subheading mt-5 text-2xl">{title}</h2><p className="mx-auto mt-3 max-w-xl text-sm text-[var(--mythoria-text-muted)]">{description}</p>{action && <div className="mt-6 flex justify-center">{action}</div>}</section>; }
