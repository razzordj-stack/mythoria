import type { ReactNode } from "react";
export function MythoriaSectionTitle({ title, description, icon, action, className = "" }: { title: string; description?: string; icon?: ReactNode; action?: ReactNode; className?: string }) { return <div className={["flex items-start justify-between gap-4", className].join(" ")}><div className="flex items-start gap-3">{icon && <span aria-hidden="true" className="mt-0.5 text-xl text-[var(--mythoria-neon-soft)]">{icon}</span>}<div><h2 className="mythoria-subheading text-xl sm:text-2xl">{title}</h2>{description && <p className="mt-1 text-sm text-[var(--mythoria-text-muted)]">{description}</p>}</div></div>{action}</div>; }
