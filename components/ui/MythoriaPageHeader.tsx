import type { ReactNode } from "react";
export function MythoriaPageHeader({ eyebrow, title, description, actions, className = "" }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode; className?: string }) { return <header className={["flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between", className].join(" ")}><div>{eyebrow && <p className="text-xs font-bold tracking-[.18em] text-[var(--mythoria-green-bright)]">{eyebrow}</p>}<h1 className="mythoria-heading mt-1">{title}</h1>{description && <p className="mt-3 max-w-3xl text-sm text-[var(--mythoria-text-muted)] sm:text-base">{description}</p>}</div>{actions && <div className="flex flex-wrap gap-3">{actions}</div>}</header>; }
