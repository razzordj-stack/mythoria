import type { ReactNode } from "react";
export function MythoriaPageHeader({ eyebrow, title, description, actions, className = "" }: { eyebrow?: string; title: string; description?: string; actions?: ReactNode; className?: string }) { return <header className={["mythoria-page-header flex flex-col gap-4 lg:flex-row lg:items-end lg:justify-between", className].join(" ")}><div className="min-w-0">{eyebrow && <p className="text-[11px] font-bold tracking-[.2em] text-[var(--mythoria-green-bright)]">{eyebrow}</p>}<h1 className="mythoria-heading mt-1">{title}</h1>{description && <p className="mt-2 max-w-3xl text-sm leading-6 text-[var(--mythoria-text-muted)]">{description}</p>}</div>{actions && <div className="flex flex-wrap gap-2">{actions}</div>}</header>; }
