"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";import {AuthShell} from "@/components/layout/AuthShell";import {MythoriaAlert} from "@/components/ui/MythoriaAlert";
export default function ResetPassword(){const[password,setPassword]=useState("");const[confirm,setConfirm]=useState("");const[message,setMessage]=useState("");const[success,setSuccess]=useState(false);const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setSuccess(false);if(password.length<8){setMessage("Mindestens 8 Zeichen erforderlich.");return}if(password!==confirm){setMessage("Die Passwörter stimmen nicht überein.");return}setBusy(true);const{error}=await createClient().auth.updateUser({password});setMessage(error?error.message:"Passwort erfolgreich geändert.");setSuccess(!error);setBusy(false)}return <AuthShell title="Neues Passwort" description="Lege einen neuen Schlüssel für deine Chronik fest."><form onSubmit={submit} className="space-y-5"><label htmlFor="new-password" className="block text-sm font-semibold text-[var(--mythoria-gold-light)]">Neues Passwort<input id="new-password" required type="password" autoComplete="new-password" value={password} onChange={e=>setPassword(e.target.value)} className="mythoria-input mt-2" /></label><label htmlFor="confirm-password" className="block text-sm font-semibold text-[var(--mythoria-gold-light)]">Passwort bestätigen<input id="confirm-password" required type="password" autoComplete="new-password" value={confirm} onChange={e=>setConfirm(e.target.value)} className="mythoria-input mt-2" /></label>{message&&<MythoriaAlert variant={success?"success":"error"}>{message}</MythoriaAlert>}<button disabled={busy} className="mythoria-button-primary w-full">{busy?"Speichern …":"Passwort speichern"}</button></form><Link href="/login" className="mt-6 block text-center text-sm font-bold text-[var(--mythoria-gold-light)]">Zur Anmeldung</Link></AuthShell>}
