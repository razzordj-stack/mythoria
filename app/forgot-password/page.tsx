"use client";
import Link from "next/link";import {FormEvent,useState} from "react";import {createClient} from "@/lib/supabase/client";import {AuthShell} from "@/components/layout/AuthShell";import {MythoriaAlert} from "@/components/ui/MythoriaAlert";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[message,setMessage]=useState("");const[busy,setBusy]=useState(false);async function submit(e:FormEvent){e.preventDefault();setBusy(true);const{error}=await createClient().auth.resetPasswordForEmail(email,{redirectTo:window.location.origin+"/reset-password"});setMessage(error?error.message:"Wenn ein Konto existiert, wurde eine Reset-E-Mail versendet.");setBusy(false)}return <AuthShell title="Passwort vergessen" description="Wir senden dir einen sicheren Link zum Zurücksetzen."><form onSubmit={submit} className="space-y-5"><label htmlFor="reset-email" className="block text-sm font-semibold text-[var(--mythoria-gold-light)]">E-Mail<input id="reset-email" required type="email" autoComplete="email" value={email} onChange={e=>setEmail(e.target.value)} className="mythoria-input mt-2" /></label>{message&&<MythoriaAlert>{message}</MythoriaAlert>}<button disabled={busy} className="mythoria-button-primary w-full">{busy?"Wird gesendet …":"Reset-Link senden"}</button></form><Link href="/login" className="mt-6 block text-center text-sm font-bold text-[var(--mythoria-gold-light)]">Zurück zur Anmeldung</Link></AuthShell>}
